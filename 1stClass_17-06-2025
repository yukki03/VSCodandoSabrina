Algoritimo "Calcular Média"
// Descrição: 
Var
   n1,n2:real
Inicio
   // Apontamentos:
   // '//comentário' <- para comentários de uma linha
   // '<- atribuição' <- para atribuir valores às variáveis


   escreva("Calculadora de Média") // Exibe o título do programa
   escreva("Digite o valor do primeiro número: ") // Solicita ao usuário que digite um valor
   leia(n1)
   escreva("Digite o valor do segundo número: ") // Solicita ao usuário que digite outro valor
   leia(n2)
   escreva("A média é: ", (n1+n2)/2) // Calcula e exibe a média dos dois números

FimAlgoritmo // Fim do algoritmo